<UserControl x:Class="WinApp.Views.DangerItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinApp.Views"
             xmlns:vst="clr-namespace:Vst.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Padding="10" 
            BorderThickness="1"
            BorderBrush="LightGray"
            Background="White"
            CornerRadius="4"
            Margin="0, 2">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition  Width="40" />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Border x:Name="Icon" />

                <TextBlock Text="{Binding Path=Name}" Grid.Column="1" FontSize="14" />
                <TextBlock Text="{Binding Path=Time, StringFormat='dd/MM, HH:mm'}"
                           Grid.Column="2" HorizontalAlignment="Right" 
                           Foreground="#666" FontSize="14" />

                <TextBlock Text="{Binding Path=Address}" 
                           Grid.Row="1" Grid.Column="1"
                           FontSize="12" 
                           Foreground="#666" />
            </Grid>
        </StackPanel>
    </Border>

</UserControl>
