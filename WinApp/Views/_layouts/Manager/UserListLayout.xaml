<UserControl x:Class="WinApp.Views.UserListLayout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinApp.Views"
             xmlns:vst="clr-namespace:Vst.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid>
            <Border x:Name="LeftPanel">
                <StackPanel HorizontalAlignment="Center">
                    <vst:HeaderText HeaderSize="4" Text="{Binding Path=Name}" />
                    <vst:HeaderText HeaderSize="6" Text="Thông tin của người dùng sẽ bị xóa khỏi hệ thống" />
                </StackPanel>
            </Border>
            
            <vst:Button Width="60" 
                        Height="60" 
                        Text="thêm"
                        CornerRadius="60"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="40"
                        x:Name="BtnAddUser"
                        />
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="flyingColumn" 
                              Tag="400"
                              Width="0" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1" x:Name="RightPanel">
                <Border Background="{StaticResource BannerBrush}" Opacity="0.9" />
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <vst:GridLayout
                        Padding="20"
                        Background="White"
                        CornerRadius="4">
                        <StackPanel>
                            <vst:HeaderText Foreground="#333"
                                    HeaderSize="5"
                                    Text="{Binding Path=Name}" />
                            <vst:HeaderText Foreground="#333" 
                                            HeaderSize="6"
                                            Padding="0"
                                            Text="{Binding Path=ObjectId}" />
                        </StackPanel>

                        <vst:CloseButon x:Name="BtnClose" 
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Background="White"
                                        Foreground="Gray" />

                    </vst:GridLayout>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center"
                                Margin="10"
                                Grid.Row="1">
                        <vst:Button Text="chọn thiết bị" 
                                    CornerRadius="4"
                                    Grid.Column="0"
                                    HorizontalAlignment="Center"
                                    Padding="30 10"
                                    x:Name="btnSelectDevice" />
                        <vst:Button Text="xóa người dùng" 
                                    CornerRadius="4"
                                    Background="Red" 
                                    HorizontalAlignment="Center"
                                    Padding="30 10"
                                    Grid.Column="1"
                                    x:Name="BtnDelete"/>
                    </StackPanel>

                    <vst:GridLayout Grid.Row="2"
                        Padding="20"
                        Background="White"
                        CornerRadius="4">
                        <vst:MyScrollViewer x:Name="SelectorContent">
                        </vst:MyScrollViewer>
                    </vst:GridLayout>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
