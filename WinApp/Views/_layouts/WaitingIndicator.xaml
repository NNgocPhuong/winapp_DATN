<UserControl x:Class="WinApp.Views.WaitingIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WinApp.Views"
             mc:Ignorable="d" Width="100" Height="100" >

    <Viewbox>
        <Grid Height="200" Width="200">
            <!-- Vòng tròn nền -->
            <Ellipse Stroke="LightGray" 
                 StrokeThickness="20"/>

            <!-- Vòng cung xoay -->
            <Grid>
                <Path Stroke="DodgerBlue" 
                          StrokeThickness="22"
                          Data="M 100, 9 A 100,100 0 0 1 191, 100"
                          >
                    <Path.RenderTransform>
                        <RotateTransform CenterX="100" CenterY="100" x:Name="SpinnerRotate"/>
                    </Path.RenderTransform>
                    <Path.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                     From="0" To="360" Duration="0:0:2" RepeatBehavior="Forever" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Path.Triggers>
                </Path>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>
